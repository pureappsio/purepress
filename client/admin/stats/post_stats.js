Template.postStats.helpers({

    // conversion: function() {
    //     if (Stats.findOne({ type: 'subscribe', postId: this._id }) && Stats.findOne({ type: 'visit', postId: this._id })) {
    //         return (Stats.find({ type: 'subscribe', postId: this._id }).fetch().length / Stats.find({ type: 'visit', postId: this._id }).fetch().length * 100).toFixed(0);
    //     } else {
    //         return 0;
    //     }
    // }

});
